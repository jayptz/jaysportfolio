// Custom framer-motion wrapper to avoid Next.js 15 export * issues
import {
  motion,
  AnimatePresence,
  useMotionValue,
  useSpring,
  useTransform,
  useInView,
  MotionProps,
  MotionValue,
} from 'framer-motion'

export {
  motion,
  AnimatePresence,
  useMotionValue,
  useSpring,
  useTransform,
  useInView,
  type MotionProps,
  type MotionValue,
} 